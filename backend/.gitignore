# Ignore node_modules (dependencies get installed from package.json)
node_modules/

# Ignore environment variables
.env
.env.*
!.env.example  # Keep an example file if needed

# Other standard ignores (as previously shared)